image: busybox

pages:
  stage: deploy
  script:
    - mkdir public
    - cp -r * public/
    - rm -rf public/public # Prevent infinite recursion if public exists
  artifacts:
    paths:
      - public
  only:
    - master

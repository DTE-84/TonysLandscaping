image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r assets public/
    - cp index.html style.css script.js public/
    - ls -la public/
  artifacts:
    paths:
      - public
  only:
    - main
    - master
